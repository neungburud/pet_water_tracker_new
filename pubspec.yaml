name: pet_drinking_monitor
description: แอปพลิเคชันติดตามการดื่มน้ำของสัตว์เลี้ยง.
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  shared_preferences: ^2.2.1
  mqtt_client: ^10.0.0
  fl_chart: ^0.63.0
  flutter_local_notifications: ^15.1.0+1
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true